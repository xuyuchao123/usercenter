select * from
        (select *
        from SYS_REPORT_QUEUE_INFO a
        left join (select BILLNO from print_affirm@sgps where trunc(printtime) = trunc(sysdate)) b
        on a.BIG_LADING_BILL_NO = b.BILLNO) c
        where c.BILLNO is null